<Window
    x:Class="test.Window1"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:test"
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="Window1"
    Width="1000"
    Height="650"
    mc:Ignorable="d" WindowStyle="None">
    <Grid Background="#FF121212">
        
        <materialDesign:DrawerHost
            Margin="55,0,-0.4,0" 
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            BorderBrush="{DynamicResource MaterialDesignDivider}"
            BorderThickness="0" Height="620" Width="994">
            <materialDesign:DrawerHost.LeftDrawerContent>
                <StackPanel Margin="16">
                    <TextBlock Margin="4" HorizontalAlignment="Center">
                        LEFT FIELD
                    </TextBlock>
                    <Button
                        Margin="4"
                        HorizontalAlignment="Center"
                        Command="{x:Static materialDesign:DrawerHost.CloseDrawerCommand}"
                        CommandParameter="{x:Static Dock.Left}"
                        Style="{DynamicResource MaterialDesignFlatButton}">
                        CLOSE THIS
                    </Button>
                    <Button
                        Margin="4"
                        HorizontalAlignment="Center"
                        Command="{x:Static materialDesign:DrawerHost.CloseDrawerCommand}"
                        Style="{DynamicResource MaterialDesignFlatButton}">
                        CLOSE ALL
                    </Button>
                </StackPanel>
            </materialDesign:DrawerHost.LeftDrawerContent>
          
            <Grid MinWidth="480" MinHeight="480">
                <Grid HorizontalAlignment="Center" VerticalAlignment="Center">
                    <Grid.RowDefinitions>
                        <RowDefinition />
                        <RowDefinition />
                        <RowDefinition />
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition />
                        <ColumnDefinition />
                        <ColumnDefinition />
                    </Grid.ColumnDefinitions>
                    <Button
                        Grid.Row="1"
                        Grid.Column="0"
                        Margin="-430,-312,438,280"
                        Command="{x:Static materialDesign:DrawerHost.OpenDrawerCommand}"
                        CommandParameter="{x:Static Dock.Left}">
                        <materialDesign:PackIcon Kind="Menu" />
                    </Button>



                </Grid>
            </Grid>
        </materialDesign:DrawerHost>

        <Grid Width="48" HorizontalAlignment="Left">
            <Button Margin="0,48,0,540">

                <materialDesign:PackIcon Kind="Soccer" />
            </Button>
            <Button Margin="0,110,0,501.2">
                <materialDesign:PackIcon Kind="SoccerField" />
            </Button>
            <Button Margin="0,560,0,51.2">
                <materialDesign:PackIcon Kind="Language" />
            </Button>
            <Button Margin="0,163,0,448.2"  >
                
                    <materialDesign:PackIcon Kind="GamepadVariantOutline" />
            </Button>
            <Button>
                <materialDesign:PackIcon Kind="MapMarkerRadius" />
            </Button>

        </Grid>
    </Grid>
</Window>
